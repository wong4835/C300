#include <stdio.h>

main()
{
    int x;
    int y;

    x = 20;
    y = 10;

    if(x > y)
    {
        printf ("x has a value greater than y");
    }
    else
    {
        printf ("The value of x is less than or equal to y.");
    }
}
