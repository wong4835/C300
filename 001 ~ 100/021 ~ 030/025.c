#include <stdio.h>

main()
{
    int x = 1;
    int y = 2;
    int z = 3;

    if(x == y)printf("x is same the y.\n");
    if(x != y)printf("x is not same the y.\n");
    if(x > y)printf("x is bigger the y.\n");
    if(x < y)printf("x is smaller the y.\n");
    if(x >= y)printf("y is bigger or same the x.\n");
    if(x <= y)printf("y is smaller or same the x.\n");
}
